eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
#  endpoint:
#    health:
#      show-details: always
info:
  author: Daniel Fontoura
  mail: absolutdsf@gmail.com
server:
  port: ${PORT:9999}
message: Seja bem-vindo
